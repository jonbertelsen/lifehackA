package dat.backend.control.unitConverter;
/**
 * The purpose of these two classes (converterCalc, unitConverter) is to make it possible for the user to
 * enter a specific number in any of the 8 unit types. It will then automatically converts the userinput into
 * the selected output unit type - and show the user the output.
 */